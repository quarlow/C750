-- Due to the errors and issues I found when creating the tables, and altering the schema, I frequently dropped the tables 
-- and started over.  I used this script to drop all the tables in one action.

drop table osm.ways_tags;
drop table osm.ways_nodes;
drop table osm.nodes_tags;
drop table osm.nodes;
drop table osm.ways;
